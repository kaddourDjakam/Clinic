<Window x:Class="Clinik.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clinik.View"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBlock Text="Password:" Margin="0,10,0,5"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBox Text="{Binding Username}" Width="200" Height="25" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="200" Height="25" VerticalAlignment="Center" Margin="0,10,0,5"/>
            <Button Content="Login" Command="{Binding LoginCommand}" Width="100" Height="30" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
